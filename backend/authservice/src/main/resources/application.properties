spring.application.name=authservice
server.port=8083

# Eureka (hyphen in service-url!)
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.hostname=localhost
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
eureka.instance.lease-renewal-interval-in-seconds=30
eureka.instance.lease-expiration-duration-in-seconds=90

# MySQL
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/auth_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.open-in-view=false

# Actuator
management.endpoints.web.exposure.include=health,info

# Dev logging
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql=trace

# JWT (dev only; move to ENV)
# at least 64 bytes of entropy (Base64 is fine)
app.jwt.secret=ThisIsA_DEV_ONLY_SuperLongHS256_SecretKey_change_me_0123456789_abcdefghijklmnopqrstuvwxyz_XYZ
app.jwt.issuer=spc-auth
app.jwt.expiryMinutes=6000000

# Cookie (if/when you issue cookies)
app.cookie.name=SPC_JWT
app.cookie.domain=localhost
app.cookie.httpOnly=true
app.cookie.secure=false
app.cookie.sameSite=Lax

cloudinary.cloud-name=di6tphz69
cloudinary.api-key=935213368721172
cloudinary.api-secret=xqUQwqml5IvXxtteu61IsklUqVo
storage.cloudinary.folder-prefix=employees/



# ====== MAIL CONFIG (for OTP emails) ======
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=youraddress@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.default-encoding=UTF-8
