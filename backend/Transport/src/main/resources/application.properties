# --- Server ---
server.port=8081

# --- DB (MySQL local dev) ---
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/transport_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Joy12365
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.open-in-view=false

# (Optional: helpful SQL logging for debugging)
# logging.level.org.hibernate.SQL=debug
# logging.level.org.hibernate.orm.jdbc.bind=trace

# --- JSON (CONFIGURED IN CODE via JacksonConfig) ---
# spring.jackson.default-property-inclusion=non_null
# spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss.SSS'Z'
# spring.jackson.time-zone=UTC

# --- DevTools (optional) ---
spring.devtools.restart.enabled=true